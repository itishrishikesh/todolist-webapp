pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Build Pipeline DEMO"
            }
        }
        stage('deploy') {
            steps {
                echo "DEeploy Pipeline DEMO"
            }
        }
    }
}